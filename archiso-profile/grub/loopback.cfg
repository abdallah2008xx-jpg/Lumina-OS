# Search for the ISO volume
search --no-floppy --set=archiso_img_dev --file "${iso_path}"
probe --set archiso_img_dev_uuid --fs-uuid "${archiso_img_dev}"

if [ "${grub_platform}" == 'efi' ]; then
    ahmados_platform='UEFI'
elif [ "${grub_platform}" == 'pc' ]; then
    ahmados_platform='BIOS'
else
    ahmados_platform="${grub_cpu}-${grub_platform}"
fi

default=ahmados
timeout=5
timeout_style=menu

menuentry "AhmadOS (%ARCH%, ${ahmados_platform})" --class arch --class gnu-linux --class gnu --class os --id 'ahmados' {
    set gfxpayload=keep
    linux /%INSTALL_DIR%/boot/%ARCH%/vmlinuz-linux archisobasedir=%INSTALL_DIR% img_dev=UUID=${archiso_img_dev_uuid} img_loop="${iso_path}"
    initrd /%INSTALL_DIR%/boot/%ARCH%/initramfs-linux.img
}

menuentry 'System shutdown' --class shutdown --class poweroff {
    echo 'System shutting down...'
    halt
}

menuentry 'System restart' --class reboot --class restart {
    echo 'System rebooting...'
    reboot
}
